---
file: 05-本地数据库替代方案分析.md
description: YYC3 Administration P3 决策项——本地数据库（Dexie）+ 三实例 PostgreSQL 盘点（本机 PG15 + NAS PG13/PG14）与私服后端规划及落地 Runbook
author: AI Tutor <TRAE / Intelligent Application Implementation Expert>
version: v1.3.0
created: 2026-09-26
updated: 2026-09-26
status: active
tags:
  [
    p3,
    local-database,
    indexeddb,
    dexie,
    postgresql,
    nas,
    architecture,
    decision,
  ]
category: analysis
---

# 🗄️ P3 替代方案分析：本地数据库取代 Supabase

> 用户决策（2026-09-26）：放弃 Supabase 云后端激活，改为**纯本地数据架构**。
> 本文档给出选型对比、推荐路线、迁移步骤与风险边界，供实施时直接执行。

## 一、现状盘点（本地化的起点）

| 数据面                 | 现状                                   | 位置                       | 本地化后去向                        |
| ---------------------- | -------------------------------------- | -------------------------- | ----------------------------------- |
| 认证/用户库            | 无盐 SHA-256 + localStorage            | `useAuthStore`             | 本地 Profile 库（见 §4-B）          |
| 应用设置               | secure-storage(AES-GCM) + localStorage | `useSettingsStore`         | Dexie `settings` 表                 |
| AI 模型配置            | secure-storage 加密                    | `useAIModelStore`          | 保持（密钥仍走加密），落 Dexie 密文 |
| 联系人/号牌库          | React Context + localStorage           | `contacts-context`         | Dexie `contacts` 表                 |
| 任务看板               | Zustand + localStorage                 | `task-board-store`         | Dexie `tasks` 表                    |
| AI 对话历史/编辑器快照 | **已是 IndexedDB**                     | `src/multi-end/storage.ts` | 统一并入 Dexie                      |
| 设置云同步             | Supabase 适配层（冻结）                | `user-data-sync.ts`        | 改挂本地适配器（见 §5）             |

**关键资产**：`supabase-client` 是 env 双门控的，**不配置即零影响**——无需删除，未来想上云仍可回头。CSP 中 supabase 域白名单无害可保留。

## 二、选型对比（浏览器端本地数据库）

| 方案　　　　　　　　　　　　　　　 | 体积(gz)　  | 类型/查询　　　　　　　　　　 | 响应式　　　　　　　　　　　　　 | 离线PWA　　   | 成熟度　　　　　　　　　　　      | 适用判断　　　　　　　　　　　　　　  |
| ---------------------------------- | ----------- | ----------------------------- | -------------------------------- | ------------- | --------------------------------- | ------------------------------------- |
| **Dexie.js**（IndexedDB 封装）　　 | ~25KB　　　 | 强类型 + 索引查询 + 复合索引  | `liveQuery`（配合 Zustand 极佳） | ✅ 原生　　　 | ⭐⭐⭐⭐⭐ 10 年+，浏览器事实标准 | **✅ 推荐**　　　　　　　　　　　　　 |
| wa-sqlite / sql.js（SQLite WASM）  | 1-2MB WASM  | 真 SQL/JOIN/事务　　　　　　  | 需自建订阅　　　　　　　　　　　 | OPFS 持久化   | ⭐⭐⭐　　　　　　　　　　　　　  | 数据关系复杂、明确要 SQL 才选　　　　 |
| RxDB　　　　　　　　　　　　　　　 | ~100KB+　　 | 文档模型 + 丰富复制协议　　　 | 原生响应式　　　　　　　　　　　 | ✅　　　　　  | ⭐⭐⭐⭐　　　　　　　　　　　　  | 未来要做多端同步时的升级项，现在过重  |
| cr-sqlite（CRDT SQLite）　　　　　 | WASM+　　　 | SQL + 多写者 CRDT　　　　　　 | 有限　　　　　　　　　　　　　　 | ✅　　　　　  | ⭐⭐ 前沿　　　　　　　　　　　   | 试验性，不建议生产　　　　　　　　　  |
| 裸 IndexedDB（现状 storage.ts）　  | 0　　　　　 | 无查询抽象、样板代码多　　　  | 无　　　　　　　　　　　　　　　 | ✅　　　　　  | -　　　　　　　　　　　　　　     | 已有，但撑不起业务库　　　　　　　　  |

### 结论：**Dexie.js**

理由（对照本项目实际）：

1. **与 Zustand 组合是最佳实践**——`liveQuery` 天然替代手写"读库→setState"同步；
2. 25KB 且可懒加载，与 F-07 刚做的首屏瘦身方向一致（SQLite WASM 会抵消我们省下的体积）；
3. 项目数据以"实体清单+过滤"为主，无复杂 JOIN 需求；
4. 迁移机制内建（`db.version(n).stores().upgrade()`），localStorage→IndexedDB 一次性迁移有官方模式；
5. 已有 IndexedDB 使用经验（storage.ts），团队心智成本低。

## 三、目标架构

```mermaid
flowchart LR
    subgraph UI["UI 层（features 10 域）"]
        Z1[Zustand stores<br/>内存态/乐观更新]
    end
    subgraph DataLayer["数据层（新增 src/db/）"]
        DEX[(Dexie 'yyc3-db'<br/>contacts/tasks/settings/<br/>chats/snapshots/profiles)]
        LQ[liveQuery 桥]
        MIG[localStorage 一次性迁移器]
        SYNC[SyncAdapter 接口]
    end
    subgraph Storage["浏览器存储"]
        IDB[(IndexedDB)]
        LS[localStorage<br/>→降级为只读缓存]
        OPFS[OPFS<br/>大文件(可选)]
    end
    subgraph Optional["可选出口（保持插槽）"]
        EXP[JSON 导出/导入<br/>跨设备手动同步]
        SUPA[Supabase 适配器<br/>env 门控,休眠保留]
    end
    UI <--> LQ <--> DEX
    Z1 -- 写入 --> DEX
    MIG --> DEX
    DEX <--> IDB
    SYNC <--> EXP
    SYNC -.可选.-> SUPA
```

## 四、实施路线（三阶段，每阶段可独立交付）

### Phase A：数据收口（约 2~3 人日）

1. `pnpm add dexie`（经冷却期）+ 新建 `src/db/db.ts`：

```ts
// src/db/db.ts —— 统一本地库(F-12 后新增)
import Dexie, { type EntityTable } from 'dexie'
import type { Contact } from '@/features/customer/pages/number-database-data'

export const db = new Dexie('yyc3-db') extends Dexie {} // 示意,实际见下
export const yyc3db = new Dexie('yyc3-db')
yyc3db.version(1).stores({
  contacts: 'id, name, stage, starred, tags, updatedAt',
  tasks: 'id, status, priority, dueDate',
  settings: 'key',            // key-value,值仍可为加密密文
  chatSessions: 'id, model, updatedAt',
  snapshots: 'id, kind, updatedAt',
  profiles: 'username',        // 本地账户库(Phase B)
})
export type ContactTable = EntityTable<Contact, 'id'>
```

1. **迁移器**（启动一次性，成功后打标记）：读各 localStorage 键 → bulkPut → 旧键改名 `__migrated_*`（保留一版兜底）；
2. 逐域替换：contacts-context → liveQuery；task-board-store persist 中间件换 Dexie；
3. `storage.ts` 的 chat/snapshot 表**导入或直接指向同一 Dexie 实例**（消除双 IndexedDB 库）。

### Phase B：本地账户库（约 1~2 人日，替换无盐 SHA-256）

- `profiles` 表存 `{username, salt, verifier, vaultKeyEnc, createdAt}`；
- 登录：PBKDF2(password, salt, 150k) → 派生密钥解 `vaultKeyEnc` 得会话密钥（WebCrypto，密钥不出内存）；
- "记住我"：vaultKey 存 **IndexedDB(非 sessionStorage)**——顺带修复审计 SEC-N2 的"跨标签失效"缺陷；
- 保留演示横幅（本地库仍是演示级边界：同源 JS 可读，但比明文口令哈希强一个量级）。

### Phase C：同步出口（约 1 人日）

- 定义 `SyncAdapter { pull(): Promise<Snapshot>, push(s: Snapshot): Promise<void> }`；
- 本地默认实现 = JSON 文件导出/导入（下载/上传，PWA 离线友好）；
- `UserDataSync` 挂载件改注入 LocalSyncAdapter（登录即 pull 水合本地库，变更防抖 push 到导出暂存）——**复用 F-01 已接好的生命周期**，只换适配器实现；
- Supabase 适配器代码原地保留（env 门控），文档标注"休眠可选"。

## 五、获得与放弃（决策记录）

| 维度 | 获得                                      | 放弃/边界                                            |
| ---- | ----------------------------------------- | ---------------------------------------------------- |
| 隐私 | 数据不出浏览器，零服务端成本              | ❌ 跨设备自动同步（降级为导出/导入或未来 RxDB 复制） |
| 安全 | 消除无盐 SHA-256；密钥不出内存            | ⚠️ 仍非真实安全边界（同源 XSS 可读），演示横幅保留   |
| 运维 | 无账号/无计费/无 RLS 维护                 | ❌ 服务端 RBAC/审计（RBAC 退化为本地角色字段）       |
| 性能 | IndexedDB 容量 GB 级，大列表索引查询      | 清浏览器数据即全丢 → 导出提醒 UI 必须做              |
| 工程 | 复用既有 env 门控与 UserDataSync 生命周期 | Supabase 迁移 SQL（0001）归档为可选资产              |

## 六、验收清单

- [ ] `yyc3-db` 单一 IndexedDB 库，5+ 表带索引，`db.version` 迁移链就绪
- [ ] localStorage 业务键迁移完成并打 `__migrated_` 标记；二次启动零迁移开销
- [ ] 联系人/任务/设置读写全部过 Dexie；liveQuery 驱动 UI（删手动 setState 同步）
- [ ] 本地登录走 PBKDF2+vault，"记住我"跨标签页生效
- [ ] JSON 导出/导入闭环（导出文件可完整恢复到另一浏览器）
- [ ] E2E 补一条：建联系人→导出→清库→导入→断言还原
- [ ] 全口径覆盖率（`pnpm test:coverage:all`）因 db 层纯函数可测而提升 ≥3pp

---

## 七、PostgreSQL 实例实测盘点（2026-09-26 补充 · v1.2 勘误重写）

> 实测方式：本机直连 `psql -h 127.0.0.1 -p 5433`（trust 认证）；NAS 经 ssh 别名 `yyc3-45`（Tailscale `100.65.172.88:9557`，用户 `YYC3`）远程执行 NAS 本机 psql。
> **二次勘误（重要，推翻 v1.1 结论）**：NAS 上实为**双实例并存、同抢 5432 端口**——QTS 系统库 **PG 13.13**（仅监听 `127.0.0.1:5432`）与 qpkg **PostgreSQL_okm 14.15**（`/Volume2/@apps/PostgreSQL_okm/`，监听 `192.168.3.45:5432` + `[::1]:5432`）。此前经 ssh 隧道连 `127.0.0.1:5432` 只到达系统库，看到的"PG13 空库"是影子；**真实业务数据（含 19.7 万行向量库）全在 PG14 实例中**。

### 7.1 实例 A：本机 PG 15.15（Homebrew，端口 5433）

**15 个业务库，全部为 schema-only（实测 `pg_stat_user_tables` 全库 0 行）——是历次项目设计沉淀的"数据库架构资产库"，无生产数据。**

| 数据库 | 大小 | 表结构要点（public schema） | 与本项目关系 |
| --------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| **yyc3_saas** | 8.3MB | tenants / accounts / users / tenant_users / sessions / verification_tokens / support_tickets / workflow_executions(+step) / agent_memories / memory_consolidation_logs / operation_logs | ⭐⭐⭐ **最佳后端底座**：多租户+认证会话+工单+工作流+智能体记忆，一套完整企业 SaaS 架构 |
| **yyc3_ai_call** | 8.8MB | \_prisma_migrations / campaigns / campaign_customers / customers / forms / form_submissions / tasks / call_records / users / analytics_metrics | ⭐⭐⭐ **业务域一一对应**：营销活动/客户/表单/任务/通话记录 = 前端 customer+marketing+conversation 域；Prisma 管理，最适合 Node BFF 直接复用 |
| **yyc3_api** | 11MB | knowledge_bases / documents / document_chunks / qa_pairs / model_registry / usage_log / search_history | ⭐⭐ **AI 能力层**：模型注册表 + 知识库分块 + 用量日志（配额/计费基础） |
| **yyc3_matrix** | 8.6MB | agents / nodes / models / inference_logs / operations / patrols / alerts / security_scans / performance_metrics / ai_suggestions / files | ⭐⭐ **智能体运行时**：节点巡检/推理日志/安全扫描（对应"家庭智能体矩阵"项目） |
| **yyc3_33** | 15MB | ai_assistant_log / ai_chatbot_log / ai_content | image | nlp | rpa | \_task / ai_recommendation + analysis_customer/goods/profit/sales + base_goods_category / employee_skill / sys_app / data_lineage | ⭐ 大而全的 ERP 风格 AI+分析库（33 号项目），可选择性借鉴 analysis\_\* 报表设计 |
| yyc3_my | 16MB | 仅 1 表 knowledge_documents | 名字对应本项目包名（@yyc3/my-mgmt）但**无实质架构**，可弃用或重建 |
| yyc3_core | 9.6MB | 仅 1 表 role_permissions | 仅 RBAC 雏形 |
| yyc3_aify / yyc3_dev / yyc3_mcp / yyc3_vpn / yyc3_music / okm / dorisoy_pos | 7~40MB | 各异 | 与本项目无直接关系（其他项目线） |

### 7.2 实例 B/C：NAS yyc3-45 双实例（同端口 5432，不同监听地址）

#### B1. PG 13.13（QTS 系统库，仅监听 127.0.0.1:5432）

| 数据库   | 大小  | 内容                                   | 判断                                |
| -------- | ----- | -------------------------------------- | ----------------------------------- |
| yyc3_gpt | 7.9MB | model_registry、usage_log 两张**空表** | PG14 侧同名库的"影子残留"，早期误配 |
| yyc3_kb  | 7.8MB | **完全空**                             | 同上                                |
| postgres | 8.2MB | 默认库                                 | -                                   |

QTS 系统自带、归 NAS 系统管理——**不动、不用、不迁**。

#### B2. PostgreSQL_okm **14.15**（qpkg，192.168.3.45:5432）⭐ 真实数据资产

`data_directory=/Volume2/@apps/PostgreSQL_okm/data`；已装扩展：**pgvector 0.6.2**、pg_trgm 1.6、btree_gin 1.3、hstore 1.8；角色体系完整：`yyc3_admin`(超管)/`yyc3_app`/`yyc3_mcp`/`yyc3_reader`(最小权限)/`replicator`(复制)，另有 postgres/yanyu/YYC-GLM/Family-YQ 超管。

| 数据库          | 大小       | 实测内容（精确 count）                                                                                                                                                                                                                                                                                         | 判断                                                         |
| --------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| **yyc3_kb**     | **4064MB** | `kb_entries` **197,558 行**（2026-04-25 ~ 06-01）：`embedding vector(4096)` + `embedding_1024 vector(1024)` 双嵌入列；source_type 分布 ts=28368/md=22816/go=17377/tsx=17371/py=16596/file=15508/json=14754/cpp=12098/sql=8421/h=6340——**代码级知识库**；堆 239MB+索引 87MB+TOAST ≈3.7GB；device_heartbeat=6 行 | ⭐⭐⭐ 现成 RAG 底座，**绝不重建**；本项目 KB 功能可直接挂载 |
| **yyc3_mcp**    | 12MB       | 14 表：mcp_servers=17、mcp_call_logs=16、webhook_events=1、system_configs=5（其余空）                                                                                                                                                                                                                          | ⭐⭐ 有真实 MCP 配置数据——MCP 网关注册中心                   |
| **yyc3_gpt**    | 8.4MB      | model_registry=13、usage_log=7                                                                                                                                                                                                                                                                                 | ⭐ 模型注册表有配置数据                                      |
| yyc3_core       | 8.7MB      | `auth`/`core` 双 schema：users/roles/role_permissions（空）                                                                                                                                                                                                                                                    | 认证域骨架就绪                                               |
| yyc3_workflow   | 8.2MB      | `workflow`/`approval` schema（无表）                                                                                                                                                                                                                                                                           | 骨架                                                         |
| yyc3_monitor    | 8.2MB      | `monitor`/`metrics` schema（无表）                                                                                                                                                                                                                                                                             | 骨架                                                         |
| yyc3_audit      | 8.1MB      | `audit`/`security` schema（无表）                                                                                                                                                                                                                                                                              | 骨架                                                         |
| yyc3_ai         | 8.1MB      | `ai`/`knowledge`/`conversation` schema（无表）                                                                                                                                                                                                                                                                 | 骨架                                                         |
| yyc3_management | 8.2MB      | 仅 public，空                                                                                                                                                                                                                                                                                                  | 可作 yyc3_admin 候选宿主                                     |

**NAS 结论（推翻 v1.1）**：NAS 不只是"7×24 宿主"——**PG14 实例是承载过真实业务的数据库资产**，9 个 yyc3\_\* 库构成完整平台规划（kb/mcp/gpt/core/workflow/monitor/audit/ai/management），且向量库与 MCP 注册中心有实数据、最小权限角色已建。v1.1"从未承载业务"的误判源于 ssh 隧道误连 PG13 系统库。

### 7.3 连接拓扑与凭据（v1.2 实测修正）

```
开发端(本机 192.168.3.22/.26, TS 100.87.159.21)            生产端(NAS yyc3-45, LAN 192.168.3.45 / TS 100.65.172.88)
┌──────────────────────────────┐                          ┌────────────────────────────────────────────────┐
│ pgAdmin 4                    │    LAN 192.168.3.0/24    │ QNAP QTS + Docker 🟢 + Tailscale + Ollama×3     │
│  ├─ PG15 :5433 (trust)       │◄────────────────────────►│  ├─ PG13 系统库 :127.0.0.1:5432（空影子库）      │
│  │    15 个 yyc3 架构库       │   psql -h 192.168.3.45   │  └─ PostgreSQL_okm PG14.15 ⭐                  │
│  └─ pgAdmin "pg14" 连接 ─────┼─── LAN 直连 :5432 ──────►│     :192.168.3.45:5432 + [::1]:5432            │
│                              │                          │     9 个 yyc3_* 库 + pgvector（真实数据）       │
└──────────────────────────────┘                          └────────────────────────────────────────────────┘
```

- 本机 PG15：`psql -h 127.0.0.1 -p 5433 -U yanyu`（trust，无密码）
- NAS PG14（推荐路径）：LAN 直连 `psql -h 192.168.3.45 -p 5432 -U postgres`（当前 **trust** 免密）；最稳方式为 ssh 远程执行 NAS 本机 psql：`ssh yyc3-45 "/Volume2/@apps/PostgreSQL_okm/sys/bin/psql -h 192.168.3.45 -U postgres -d yyc3_kb"`
- NAS PG13（勿用）：`ssh -L 15432:127.0.0.1:5432 yyc3-45` 隧道只到系统库——v1.1 勘误即源于此误连
- `[::1]:5432` 虽属 PG14 监听，但被 pg_hba 拒绝（`no pg_hba.conf entry for host "::1"`），不可用
- 🔴 **安全告警**：PG14 对整个 LAN 网段 **trust 免密超管**——任意局域网设备可直连任意库。上生产前必须 `pg_hba.conf` 改 `scram-sha-256` + 强密码 + 网段白名单
- **硬约束**：SPA 浏览器只能讲 HTTP——**任何 PG 都必须经 BFF/API 层暴露**（PostgREST/pg 协议均不可直连）。这决定了 §八 的拓扑。

## 八、生产库规划（"本地优先 + 私服后端"双层架构）

> 结合 §一~§四的浏览器层（Dexie）与 §七的私服资源：**Dexie 仍是唯一数据面的 Phase A~C 落点**；本节 PG 规划为 **Phase D（可选演进）**——把 NAS 变成 7×24 的家庭生产后端，与浏览器层通过 §四-C 的 `SyncAdapter` 第二实现衔接。

### 8.1 目标库清单（落位于 PostgreSQL_okm PG14，v1.2 修正）

> v1.1 曾建议"新起 Docker PG16"；实测后修正：**优先落位现成 PG14 实例**（pgvector/角色体系/骨架库/向量数据全部现成），Docker PG17 仅作 EOL 后的迁移目标位。注意：**PG14 社区支持至 2026-11-13**，家庭私服可接受，但 2026 年内应排期迁移（`pg_dump` 平迁即可，无版本断代）。

| 库名            | 用途                                 | 落位方式（v1.2）                                                                                                           | 说明                                                                                                       |
| --------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| **yyc3_admin**  | 业务主库：用户/租户/设置/联系人/任务 | 在 PG14 **新建**（或启用空壳 yyc3_management 改名）；架构取自本机 yyc3_saas + yyc3_ai_call（`pg_dump --schema-only` 模板） | 本项目唯一权威写库；与浏览器 Dexie 双向同步                                                                |
| **yyc3_cache**  | 缓存：AI 响应缓存/会话热点/限流计数  | 以 schema 落位：`yyc3_admin.cache_*` 即可起步                                                                              | `pgcrypto`+TTL 字段；并发上来后换 Redis 容器（NAS Docker 已运行）；本项目 ai-proxy mock 缓存表结构可直接搬 |
| **yyc3_kb**     | 知识库/向量检索                      | **复用现库（197,558 行向量已在，绝不重建）**；先核实 4096/1024 维嵌入的来源模型与检索精度                                  | pgvector 0.6.2 已装；`embedding vector(4096)` 全量列 + `embedding_1024` 轻量检索列，代码级 KB 现成         |
| **yyc3_audit**  | 审计与用量                           | 启用现成 `audit`/`security` 空 schema；并入 yyc3_gpt.usage_log 既有 7 条数据                                               | AI 调用/配额/审计单一事实源，给"AI 治理中枢"供数                                                           |
| **yyc3_bgtask** | 后台任务队列                         | 启用现成 yyc3_workflow 骨架（workflow/approval schema）或 pg-boss 模式                                                     | 批量导入/定时任务/流水线（skipLocked 模式）                                                                |

> Redis vs PG 缓存的取舍不变：家庭规模（≤10 并发）PG 单库足够；Redis 仅在出现"多服务共享缓存/发布订阅"需求时引入，避免过早复杂化。
> 另：`yyc3_mcp`（17 个 MCP server 注册）与 `yyc3_gpt`（13 个模型注册）现成数据可直接为后续 MCP 网关/模型路由功能供数。

### 8.2 服务拓扑（Phase D 落地形态）

```mermaid
flowchart LR
    subgraph Browser["浏览器（PWA，admin.yyc3.vip 或内网直连）"]
        DEX[(Dexie yyc3-db)]<->API[BFF Client<br/>fetch + 防抖同步]
    end
    subgraph NAS["NAS yyc3-45（LAN 192.168.3.45 / TS 100.65.172.88）"]
        BFF["Node BFF（Docker）<br/>:8787 · Hono/Express<br/>JWT + 限流 + ai-proxy"]
        PG[(PostgreSQL_okm PG14.15 🟢<br/>yyc3_admin/cache/kb/audit<br/>已含 19.7万行向量 + pgvector)]
        RD[(Redis 7 可选<br/>yyc3-redis)]
        OLL[Ollama 🟢 3 模型]
    end
    Browser -- HTTPS over Tailscale/内网 --> BFF
    BFF --> PG
    BFF -.缓存.-> RD
    BFF -.本地推理.-> OLL
```

要点：

1. **BFF 是必经层**（浏览器不能直连 PG）——复用 `docs/design-references/` 的 edge-proxy 思路 + 本项目 ai-proxy-service 的路由契约，Node 实现放 NAS Docker；
2. **认证**：yyc3_saas.sessions/verification_tokens 架构即开即用；浏览器端 Phase B 的 vault 密钥体系可平滑升级为 JWT+refresh；
3. **同步**：`SyncAdapter` 增加 `RestSyncAdapter`（pull=GET /sync?since=cursor，push=PATCH batch），与 JSON 导出适配器并存，由 env 选择；
4. **安全前置**：PG14 现状为 **LAN 全网段 trust 免密超管**（§7.3 告警）——上生产前必须 `pg_hba.conf` 收敛为 `scram-sha-256` + 强密码 + 仅 BFF 容器网络/白名单网段可达，并为 `yyc3_app` 等最小权限角色设密启用。

### 8.3 分阶段决策树

```
Phase A~C（已定）：浏览器 Dexie 单机闭环 ── 需要跨设备/多端? ──否──► 维持现状(导出/导入)
        │是
Phase D1（0.5 天）：PG14 加固（pg_hba scram+白名单+角色设密）+ 新建 yyc3_admin（导入 yyc3_saas/ai_call 架构模板）
Phase D2（1~2 天）：Node BFF（认证+CRUD+ai-proxy 转发 Ollama/云模型）+ CSP 补 BFF 域
Phase D3（0.5 天）：RestSyncAdapter 接入 UserDataSync 生命周期（与 F-01 同一挂载点）
Phase D4（按需）：yyc3_kb 向量检索接口化（19.7万行现成数据）+ Redis 缓存容器 + 审计面板
```

### 8.4 本机 PG15（5433）的定位

- **不做生产库**（笔记本非 7×24）——定位为**开发/集成测试库**：BFF 在本机开发时 `DB_URL=postgres://yanyu@127.0.0.1:5433/yyc3_admin_dev`（trust 免密）；上 NAS 前先在本机过迁移与 E2E；
- 15 个存量架构库即"设计资产目录"：新库一律从 `yyc3_saas`+`yyc3_ai_call` **取 schema 模板**（`pg_dump --schema-only`），其余库冻结不动。

## 九、落地操作指导手册（Runbook · v1.3 新增）

> 本节把 §八 规划转为**命令级执行流程**。所有命令基于 §七 实测路径；`NAS_PSQL` 为 NAS 本机 okm 客户端，`ssh yyc3-45` 别名可用（Tailscale `100.65.172.88:9557`）。

### 9.0 立即建议（按优先级）

| #   | 建议                     | 理由                                                                                         | 时机      |
| --- | ------------------------ | -------------------------------------------------------------------------------------------- | --------- |
| 1   | **PG14 加固（D1 前半）** | LAN 全网段 trust 免密超管 = 任意局域网设备可删 19.7 万行向量数据。**与本项目无关也该立即做** | 🔴 本周   |
| 2   | **yyc3_kb 冷备份**       | 4GB 唯一数据资产，加固前先 `pg_dump` 一份到 NAS 另一卷                                       | 🔴 加固前 |
| 3   | Phase A~C（Dexie）       | 已定方案（§四），不依赖 NAS，可先行                                                          | 🟡 按排期 |
| 4   | D1 后半~D4               | 跨设备需求出现时再启动                                                                       | 🟢 按需   |

### 9.1 快速参考（本次实测验证过的命令）

```bash
# NAS 本机执行 PG14 查询（最稳路径，绕开直连协议层问题）
ssh yyc3-45 "/Volume2/@apps/PostgreSQL_okm/sys/bin/psql -h 192.168.3.45 -U postgres -d yyc3_kb -c '<SQL>'"

# 关键路径
PG14_DATA=/Volume2/@apps/PostgreSQL_okm/data        # pg_hba.conf 在此目录下
PG14_BIN=/Volume2/@apps/PostgreSQL_okm/sys/bin       # pg_dump/pg_dumpall 同目录

# 本机模板导出（PG15，trust 免密）
pg_dump -h 127.0.0.1 -p 5433 -U yanyu --schema-only --no-owner --no-privileges yyc3_saas > saas.sql
pg_dump -h 127.0.0.1 -p 5433 -U yanyu --schema-only --no-owner --no-privileges yyc3_ai_call > ai_call.sql
```

### 9.2 Phase D1 · Step 0：加固前飞行检查（只读，必做）

```sql
-- 1. 看谁正在连（确认没有 NAS 本机服务依赖 trust——若有，先给它建专用角色）
SELECT pid, usename, datname, client_addr, application_name
FROM pg_stat_activity WHERE client_addr IS NOT NULL;

-- 2. 留存当前 hba 规则基线
SELECT type, database, user_name, address, auth_method FROM pg_hba_file_rules ORDER BY rule_number;
```

⚠️ `PostgreSQL_okm` 系 qpkg 安装，若 NAS 上有依赖它的应用（如 OKM 相关服务）经 `192.168.3.45` trust 连接，切换 scram 前须先为其建角色+密码，避免断连。

### 9.3 Phase D1 · Step 1：冷备份（数据资产投保）

```bash
# NAS 本机全量导出 yyc3_kb（4GB，约 5~10 分钟；-Fc 压缩格式）
ssh yyc3-45 "$PG14_BIN/pg_dump -h 192.168.3.45 -U postgres -Fc -d yyc3_kb \
  -f /Volume2/backup/pg/yyc3_kb_\$(date +%Y%m%d).dump"
# 建议同时 pg_dump 全局对象（角色/密码）
ssh yyc3-45 "$PG14_BIN/pg_dumpall -h 192.168.3.45 -U postgres --globals-only \
  -f /Volume2/backup/pg/globals_\$(date +%Y%m%d).sql"
```

### 9.4 Phase D1 · Step 2：角色设密 + pg_hba 收敛

```bash
# 1. 为超管与最小权限角色设强密码（密码存 NAS 密码管理器，勿入 Git）
ssh yyc3-45 "$PG14_BIN/psql -h 192.168.3.45 -U postgres -c \"
ALTER ROLE postgres   WITH PASSWORD '<强密码1>';
ALTER ROLE yyc3_admin WITH PASSWORD '<强密码2>';
ALTER ROLE yyc3_app   WITH PASSWORD '<强密码3>';\""

# 2. 备份并改写 pg_hba.conf（在 PG14_DATA 目录）
ssh yyc3-45 "cp $PG14_DATA/pg_hba.conf $PG14_DATA/pg_hba.conf.bak.$(date +%Y%m%d)"
```

`pg_hba.conf` 追加/替换规则（示意，按实情收敛网段）：

```conf
# TYPE   DATABASE      USER         ADDRESS            METHOD
host     all           postgres     127.0.0.1/32       scram-sha-256
host     all           postgres     192.168.3.22/32    scram-sha-256   # 仅开发机
host     yyc3_admin    yyc3_app     192.168.3.45/32    scram-sha-256   # 仅 BFF(NAS 本机)
host     yyc3_kb       yyc3_app     192.168.3.45/32    scram-sha-256
# 删除/注释原 trust 行；BFF 上线后进一步收紧为 Docker 网段
```

```bash
# 3. 热加载（不断连已建会话）并验证
ssh yyc3-45 "$PG14_BIN/psql -h 192.168.3.45 -U postgres -c 'SELECT pg_reload_conf();'"
# 4. 免密应被拒（期望提示 password authentication），带密应成功
psql -h 192.168.3.45 -U postgres -c "SELECT 1;"        # ← 预期要求密码
```

### 9.5 Phase D1 · Step 3：建 yyc3_admin（模板移植）

```bash
# 1. 本机导出（9.1 已备 saas.sql/ai_call.sql）→ scp 到 NAS
scp saas.sql ai_call.sql yyc3-45:/tmp/

# 2. NAS 建库并依次导入（saas 为主干；ai_call 仅取业务表，冲突表跳过）
ssh yyc3-45 "$PG14_BIN/psql -h 192.168.3.45 -U postgres \
  -c 'CREATE DATABASE yyc3_admin OWNER yyc3_admin;' \
  && $PG14_BIN/psql -h 192.168.3.45 -U yyc3_admin -f /tmp/saas.sql"

# 3. 授权最小权限角色
ssh yyc3-45 "$PG14_BIN/psql -h 192.168.3.45 -U postgres -d yyc3_admin -c \"
GRANT CONNECT ON DATABASE yyc3_admin TO yyc3_app;
GRANT USAGE ON SCHEMA public TO yyc3_app;
GRANT SELECT,INSERT,UPDATE,DELETE ON ALL TABLES IN SCHEMA public TO yyc3_app;
ALTER DEFAULT PRIVILEGES IN SCHEMA public
  GRANT SELECT,INSERT,UPDATE,DELETE ON TABLES TO yyc3_app;\""

# 4. 验证：应用角色可用、超管免密被拒
ssh yyc3-45 "$PG14_BIN/psql -h 192.168.3.45 -U yyc3_app -d yyc3_admin -c '\\dt'"
```

### 9.6 Phase D2 · BFF 容器（骨架）

```yaml
# nas: /share/DOCKER/yyc3-bff/docker-compose.yml（示意）
services:
  bff:
    image: node:22-alpine
    working_dir: /app
    command: node dist/server.js # Hono/Express，:8787
    environment:
      DB_URL: postgres://yyc3_app:<密码>@192.168.3.45:5432/yyc3_admin
      JWT_SECRET: ${JWT_SECRET} # .env 管理，勿入 Git
      OLLAMA_URL: http://192.168.3.45:11434
    ports: ['8787:8787']
    restart: unless-stopped
```

配套动作：本仓库 `index.html` CSP 补 `connect-src https://bff.yyc3.vip`（或 Tailscale 地址）；BFF 复用 ai-proxy-service 路由契约 + yyc3_saas 的 sessions 表做 JWT 签发。

### 9.7 Phase D3 · RestSyncAdapter（接入点）

```ts
// src/db/sync/rest-adapter.ts —— 与 JSON 导出适配器同接口（§四-C）
export interface SyncAdapter {
  pull(since: string): Promise<SyncBatch> // GET  /sync?since=cursor
  push(batch: SyncBatch): Promise<void> // PATCH /sync (batch)
}
// 挂载：AppProvider 内 UserDataSync 生命周期（与 F-01 同点），env 门控 VITE_SYNC_URL
```

### 9.8 验收与回滚

**验收清单（D1 完成 = 全绿）**

- [ ] 免密直连被拒（`psql` 无密码报 `password authentication failed`）
- [ ] `yyc3_app` 仅能访问 yyc3_admin/yyc3_kb，无 SUPERUSER
- [ ] `yyc3_admin` 表结构与模板一致（`\dt` 对照）
- [ ] 冷备份 .dump 可恢复到临时库验证（`pg_restore -d yyc3_kb_verify`）
- [ ] NAS 既有服务（若有）连接正常

**回滚（任一步失败）**

```bash
# pg_hba 回滚（保持会话不断）
ssh yyc3-45 "cp $PG14_DATA/pg_hba.conf.bak.<日期> $PG14_DATA/pg_hba.conf \
  && $PG14_BIN/psql -h 192.168.3.45 -U postgres -c 'SELECT pg_reload_conf();'"
# 建库失败：DROP DATABASE yyc3_admin;（新库无数据，直接删）
```

### 9.9 风险登记

| 风险                             | 概率 | 缓解                                             |
| -------------------------------- | ---- | ------------------------------------------------ |
| pg_hba 收紧断掉 NAS 既有依赖服务 | 中   | Step 0 飞行检查 `pg_stat_activity`，先建专用角色 |
| yyc3_kb 4GB 备份期间 IO 压力     | 低   | 夜间执行；`-Fc` 压缩 + nice                      |
| PG14 EOL（2026-11-13）           | 确定 | 年内排期 `pg_dump` 平迁 Docker PG17（§8.1）      |
| 模板库与本包 schema 漂移         | 中   | 导入后跑 BFF 迁移脚本对齐，以代码迁移为准        |
