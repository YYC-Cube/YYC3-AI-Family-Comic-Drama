---
file: G3-一致性预研记录-20260927.md
description: YYC³ AI 漫剧 M3 一致性专项预研留证 — TC-G3-001/002（insightface buffalo_l 真实 512 维）
author: YanYuCloudCube Team <admin@0379.email>
version: v1.3.0
created: 2026-09-27
updated: 2026-09-27
status: active
tags: [G3, pre-research, consistency, insightface, face-encoder, evidence]
category: report
language: zh-CN
related_docs: YYC3-60-测试与验收执行手册.md,G2-编排贯通验收记录-20260927.md,YYC3-AI-HANDOFF-会话推进日志-20260926.md
---

# G3 一致性专项预研记录（TC-G3-001/002 · insightface 真实 512 维）

## 基本信息

| 属性 | 值 |
| --- | --- |
| 性质 | **M3 预研**（对冲最大技术风险，无需等 G2 收口；对齐 YYC3-07 §12.3 前置条件） |
| 执行日期 | 2026-09-27 |
| 验收范围 | TC-G3-001（特征库建库）/ TC-G3-002（跨镜头一致性比对） |
| 执行方式 | `yyc3-ai-manju-studio/.venv/bin/python scripts/run_g3_cases.py` + `scripts/build_face_library.py` 直跑 |
| 执行环境 | manju-studio 独立 venv（Python 3.14）；insightface 2.0 + onnxruntime 1.30（CPUExecutionProvider）+ buffalo_l 模型包（341MB，w600k_r50 识别头=512d） |
| 素材 | randomuser 接口人像 ×2（men/85 det=0.858、women/26 det=0.875，上采样 640px）+ 各自「人脸感知裁剪」跨镜头变体（检测框外扩 1.8×、亮度 1.12、JPEG88） |

## 环境障碍与突破（预研价值留证）

| # | 障碍 | 突破 |
| --- | --- | --- |
| 1 | GitHub release HTTPS 直连被网络重置（与上游同步同因） | gh-proxy.com 镜像全量下载 buffalo_l.zip（275MB→解压 341MB），`unzip -l` 校验 5 个 onnx 齐全 |
| 2 | insightface 只认解压目录不认 zip，`FaceAnalysis` 静默重下载失败 | 手动解压至 `~/.insightface/models/buffalo_l/` 后加载成功 |
| 3 | thispersondoesnotexist 返回 HTML 挑战页 | 改用 randomuser 接口人像（CC 授权接口素材，仅预研不留仓库） |
| 4 | 首轮 hero 跨镜头相似度 -0.0017（≈0） | 定位为**固定裁剪把人脸裁出检测框 → 静默哈希降级**；改造为「人脸感知裁剪」（检测框外扩取景）+ 给 face_encoder 加 `last_mode` 逐帧留证（降级红线显式化） |

## TC-G3-001 角色特征库建库（512 维）——🟢 PASS

| 检查项 | 结果 |
| --- | --- |
| 2 角色入库（hero/villain） | ✅ |
| manifest mode=**insightface**（真实模型，非降级） | ✅ |
| 特征维度=**512** | ✅ |
| 同图二次编码余弦相似度=1.000000 | ✅ |
| feature.npy + manifest.json + index.json 三件齐 | ✅ |

manifest 样例（hero）：

```json
{"char_id": "hero", "name": "hero",
 "source_image": "/tmp/g3_faces/_build_source/hero.png",
 "dim": 512, "mode": "insightface",
 "created_at": "2026-09-27T02:06:45"}
```

## TC-G3-002 跨镜头一致性比对——🟢 PASS

相似度矩阵（512 维余弦，L2 归一化）：

| 配对 | 类型 | 相似度 | 判定 |
| --- | --- | --- | --- |
| hero 库特征 vs 跨镜头 shot2 | 同角色 | **0.9643** | ≥0.85 ✅ |
| hero shot1 vs shot2 | 同角色 | **0.9643** | ≥0.85 ✅ |
| villain 库特征 vs 跨镜头 shot2 | 同角色 | **0.8921** | ≥0.85 ✅ |
| villain shot1 vs shot2 | 同角色 | **0.8921** | ≥0.85 ✅ |
| A1×B1 / A1×B2 / A2×B1 / A2×B2 | 跨角色 | 0.0119 / -0.0013 / 0.0095 / 0.0033 | 全部 <0.85 ✅ |

- **同角色最低 0.8921、跨角色最高 0.0119**——间隔（margin）0.88，区分度极佳
- 逐帧提取模式留证：`['insightface', 'insightface', 'insightface', 'insightface']`（零哈希降级）

## TC-G3-003 一致性打回闭环（M3 主体 · anchor_guard 三段锚定）——🟢 PASS（11/11，v1.1 追加）

执行器：`scripts/run_m3_anchor.py`；实现：`backend/app/modules/consistency_engine/anchor_guard.py`（三段锚定）

**生成前 pre_anchor**：hero 锚定成功（参考特征 dim=512 + 锚定前缀注入）；未建库角色直接 BLOCKED（不进生成）。

**生成中 during_constraint**：ControlNet 姿态约束描述（桩态，真实接入待 M3 视听产能）。

**生成后 post_check 打回闭环**（核心路径实测）：

| 场景 | 输入 | 相似度 | 判定 | 结果 |
| --- | --- | --- | --- | --- |
| 错人帧（生成器产出走样） | villain 变体 vs hero 参考 | **-0.0013** | **REDRAW**（携锚定前缀重绘） | ✅ |
| 重绘后同人帧 | hero 跨镜头变体 | **0.9643** | **ACCEPT** | ✅ |
| 连续错人达上限 | 错人帧 ×2（attempts=2） | -0.0013 | **ESCALATE**（转人工） | ✅ |

- 红线实测：全程 `last_mode=insightface`（哈希降级帧会被拒判并 escalate，不进生产比对）
- MAX_ATTEMPTS=2 上限生效，超限不再无限重绘

## 结论与 M3 主体建议

1. **技术可行性实证 ✅**：insightface 512 维真实链路（检测→对齐→嵌入→建库→比对→**打回闭环**）在本地 CPU 环境全通，一致性 ≥0.85 门禁阈值在「同源跨取景」条件下余量充足（最低 0.8921）；TC-G3-001/002/003 三用例全 PASS。
2. **工程改进沉淀**：face_encoder 新增 `last_mode` 逐帧留证 + 无人脸检出时显式告警（降级红线可审计）；建议 M3 主体把 `anchor_guard` 的生成前校验同样接入 last_mode 判定，杜绝哈希向量混入生产比对。
3. **预研边界（诚实声明）**：素材为接口人像 + 同源变换变体，尚未覆盖「同角色不同服装/光影/角度大跨度」的真实多机位场景（insightface 对此类通常 0.7-0.9，anchor_guard 阈值需在真实素材上重新标定）；SyncNet/画面质检（TC-G3-004~005）与吞吐（006/007）待硬件。
4. **下一步**：真实角色图入库（M3 主体素材到位后）→ anchor_guard 三段锚定联调 → TC-G3-003 打回闭环。

## M3 视听产能首件：DramaToolGateway 桩→实（v1.2 追加 · 🟢 10/10）

执行器 `scripts/run_m3_av.py`（manju-studio venv）：

- **ComfyUIClient**（`drama_stage_adapter.py` v1.2，两份拷贝同步）：标准 SD API 最小工作流（Loader→CLIP×2→KSampler→VAEDecode→Save）→ POST /prompt → 轮询 /history → GET /view 取产物；SSRF 三道闸与 H3 客户端同构；未配置/失败回落 stub（永不断流）；env 模板入 `agent-archive/.env.example`
- **联调后端**：内置 Mock ComfyUI（stdlib HTTP :41888）——本机无 SD 模型，生成本体为预设人脸图，首帧按角色策略返回错人图（模拟身份漂移）；**客户端/HTTP/产物取回/比对闭环为真实代码路径**（诚实边界：非真实扩散模型推理）
- **anchor_guard 进真实生成链实测**：pre_anchor（hero）→ text_to_image（HTTP）→ 首帧身份漂移 sim<0.5 → **REDRAW** → 重绘同人帧 sim=0.9643 → **ACCEPT**，两轮收束；未配置回落 stub ✓
- **TC-G3-008 提示词抽检 🟢**：抽检 20 镜三要素（风格前缀+场景+画面描述）比例 **100%**（≥90%）；钩子镜头全部含「【钩子镜头】情绪强化构图」标记、非钩头无污染（`storyboard_schema.py` 已加标记）

## anchor_guard 0.85 阈值复标（v1.2 追加 · 强变换多机位素材）

执行器 `scripts/run_threshold_calibration.py`；变换族 8×2（水平翻转/旋转12°/暖调/冷调/JPEG55/裁剪60%/压暗0.72/去饱和）：

| 指标 | 实测 |
| --- | --- |
| 同角色（库参考 vs 变体）最低 | **0.9536**（B_flip）；裁剪60% 0.9712、其余 ≥0.99 |
| 跨角色最高 | **0.0249** |
| **margin** | **0.9287** |
| 检出失败（→escalate 兜底） | 1/16（A_crop60 过近裁剪人脸出框） |

**复标结论：DEFAULT_THRESHOLD=0.85 维持不变**——ArcFace 对全局光度/几何变换鲁棒性极强（全部 ≥0.95），0.85 阈值余量充足；真实风险域在生成模型的身份漂移（本变换族不覆盖），留待真实 SD 产线漂移样本持续标定。

## 真实 ComfyUI 生成本体联调（v1.3 追加 · 真图闭环 3/3）

部署与执行器 `scripts/run_comfy_real.py`（ComfyUI@79be670 + torch 2.14 MPS + DreamShaper_8_pruned 2.13GB，模型经 hf-mirror 下载；服务端口 41888）：

| 链路场景 | 实测 | 判定 |
| --- | --- | --- |
| seed=42 建库（真实 SD 产物=角色 IP 设定图） | 512×512@25steps | 库三件套齐 |
| **身份锁定重生成（同 seed 同提示词）** | **sim=1.000**（产物字节一致；ComfyUI 同参数缓存命中 ≈1s） | **ACCEPT** ✅ |
| **漂移种子（seed=777+变体提示词）** | **sim=0.4418** | **REDRAW** ✅ 真实漂移被检出 |
| **打回后锁定重绘（seed=42）** | **sim=1.000** | **ACCEPT** ✅ 闭环收束 |

- 修复留证：新版 ComfyUI SaveImage 必填 `filename_prefix`（客户端已修，两份拷贝同步）；首次 400 校验拒绝由真实服务暴露——Mock 与真实服务的差异正是本联调价值
- **TC-G3-006 真实基线（Mac 预览路由）**：单镜（512×512/25 步）**422-486s**——超出 Mac 预览 ≤120s 目标（YYC3-06 §4.1），记录为真实硬件基线；DGX NF4 ≤300s 待硬件。同种子锁定重生成经 ComfyUI 缓存 ≈1s（生产重绘路径近零成本）

## SD 漂移分布标定（v1.3 追加 · 无 LoRA 跨种子，③真实素材集）

- 同提示词跨种子（777/888/999/1111 + 变体后缀）vs 库参考：**0.4418 / 0.5306 / 0.5216 / 0.5687**（均值 ≈0.52，全部远低于 0.85）
- **生产策略结论（行业返工率 70% 根因的量化实证）**：
  1. 无 LoRA/IPAdapter 的 SD 跨镜头一致性不可用（0.44-0.57，anchor_guard 将全部打回——这正是其职责：阻止漂移进成片）
  2. **seed-lock 是 M3 当前的合法策略**（锁定种子重生成确定性 sim=1.0，且 ComfyUI 缓存下重绘近零成本）
  3. **LoRA/IPAdapter 角色训练是 M3 视听产能的必要条件**（TC-G3-002 阈值 0.85 的达成依赖它，而非调低阈值）
- 真实角色素材集：`/tmp/comfy_out/`（hero_base=IP 设定图 + 4 漂移样本），生产素材待真实 IP 入库落 NAS
