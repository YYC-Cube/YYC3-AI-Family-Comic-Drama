# PostgreSQL 复制配置
# 文件: recovery.conf (从库 yyc3-45)

standby_mode = 'on'
primary_conninfo = 'host=yyc3-33 port=5432 user=replicator password=${REPLICATOR_PASSWORD}'
trigger_file = '/tmp/postgresql.trigger.5432'
recovery_target_timeline = 'latest'
