# PostgreSQL 访问控制配置
# 文件: pg_hba.conf (主库 yyc3-33)

# ==================== 本地连接 ====================
local   all             all                                     trust
local   replication     all                                     trust

# ==================== IPv4 本地连接 ====================
host    all             all             127.0.0.1/32            md5
host    all             all             172.29.225.115/20       md5
host    all             all             192.168.3.0/24          md5

# ==================== IPv6 本地连接 ====================
host    all             all             ::1/128                 md5

# ==================== 复制连接 ====================
# yyc3-45 (从库)
host    replication     replicator      192.168.3.45/32         md5
host    all             replicator      192.168.3.45/32         md5

# yyc3-77 (第三副本)
host    replication     replicator      192.168.3.77/32         md5
host    all             replicator      192.168.3.77/32         md5

# yyc3-22 (开发机)
host    replication     replicator      192.168.3.22/32         md5
host    all             replicator      192.168.3.22/32         md5

# ==================== Docker 网络 ====================
host    all             all             172.17.0.0/16           md5
host    replication     all             172.17.0.0/16           md5

# ==================== WireGuard VPN 网络 ====================
host    all             all             10.200.0.0/24           md5
host    replication     all             10.200.0.0/24           md5
